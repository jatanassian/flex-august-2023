const someFunction = () => {
	console.log(data);
};

const anotherFunction = () => {
	console.log('Hello');
};

anotherFunction();

// Running this file will work because we're not calling someFunction
// someFunction() => that line would crash the file when running
